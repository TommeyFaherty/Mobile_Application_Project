<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:primitives="clr-namespace:Windows.UI.Xaml.Controls.Primitives;assembly=Windows.Foundation.UniversalApiContract"
             xmlns:controls="clr-namespace:Windows.UI.Xaml.Controls;assembly=Windows.Foundation.UniversalApiContract"
             x:Class="GradeTracker.AddModulePage">

    <!--#region == Resources ==-->
    <ContentPage.Resources>
        <Style x:Key="header" TargetType="Label">
            <Setter Property="FontSize" Value="Large"/>
            <Setter Property="BackgroundColor" Value="LightGray"/>
        </Style>

        <Style x:Key="warning" TargetType="Label">
            <Setter Property="TextColor" Value="Red"/>
            <Setter Property="FontSize" Value="Small"/>
        </Style>

        <Style x:Key="instruction" TargetType="Label">
            <Setter Property="TextColor" Value="Black"/>
            <Setter Property="FontAttributes" Value="Bold"/>
        </Style>

        <Style x:Key="instructionLayout" TargetType="StackLayout">
            <Setter Property="BackgroundColor" Value="OrangeRed"/>
            <Setter Property="Margin" Value="2,2"/>
        </Style>

        <Style x:Key="entryBox" TargetType="Entry">
            <Setter Property="WidthRequest" Value="200"/>
            <Setter Property="MinimumHeightRequest" Value="150"/>
        </Style>
        
    </ContentPage.Resources>
    <!--#endregion-->
    
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <StackLayout Orientation="Horizontal" Grid.Column="0" Grid.Row="0">
                <Label Style="{StaticResource header}" Text=""/>
            </StackLayout>

            <StackLayout x:Name="addModules" Orientation="Vertical" Grid.Column="0" Grid.Row="1" 
                         Grid.ColumnSpan="4" BindingContext="{Binding NewModule}">
                <StackLayout Orientation="Horizontal" >
                    <Label Text="Module: "/>
                    <Entry Style="{StaticResource entryBox}" Text="{Binding Path= module}"/>
                </StackLayout>
                <StackLayout Orientation="Horizontal">
                    <Label Text="Number of Exams: "/>
                    <Entry MinimumWidthRequest="5" Text="{Binding Path= numOfExams}"/>

                </StackLayout>
                <StackLayout Orientation="Vertical" Grid.ColumnSpan="4" Style="{StaticResource instructionLayout}">
                    <Label Style="{StaticResource instruction}" Text="Enter information on exams based on the number of exams"/>
                    <Label Style="{StaticResource instruction}" Text= "For example: if 3 exams enter 'Assesment 1,Assesment 2, Assesment 3'" />
                </StackLayout>
                <StackLayout Orientation="Horizontal">
                    <Label Text="Exam Titles: "/>
                    <Entry Style="{StaticResource entryBox}" Grid.Column="3" Text="{Binding Path= examNamesString}"/>
                </StackLayout>
                <StackLayout Orientation="Horizontal">
                    <Label Text="Exam Weight: "/>
                    <Entry Style="{StaticResource entryBox}" Grid.Column="3" Text="{Binding Path= examWeightString}"/>
                </StackLayout>
                <StackLayout Orientation="Horizontal">
                    <Label Text="Exam Percentage: "/>
                    <Entry Style="{StaticResource entryBox}" Grid.Column="3" Text="{Binding Path= examPercentString}"/>
                </StackLayout>
            </StackLayout>

            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" Grid.Column="0" Grid.Row="2">
                <Button Text="Save" Command="{Binding SaveListCommand}" Clicked="MainPage_Clicked"/>
                <Button x:Name="btnHomePage" Clicked="MainPage_Clicked" Text="Back"/>
            </StackLayout>

        </Grid>
       
    </ContentPage.Content>
  
</ContentPage>